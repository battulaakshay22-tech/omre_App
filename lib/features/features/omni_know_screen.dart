import 'package:flutter/material.dart';

class OmniKnowScreen extends StatelessWidget {
  const OmniKnowScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('OmniKnow')),
      body: const Center(child: Text('OmniKnow Content Coming Soon')),
    );
  }
}

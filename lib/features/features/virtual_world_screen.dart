import 'package:flutter/material.dart';

class VirtualWorldScreen extends StatelessWidget {
  const VirtualWorldScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Virtual World')),
      body: const Center(child: Text('Virtual World Content Coming Soon')),
    );
  }
}

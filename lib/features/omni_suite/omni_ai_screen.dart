import 'package:flutter/material.dart';

class OmniAIScreen extends StatelessWidget {
  const OmniAIScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Omni AI')),
      body: const Center(child: Text('Omni AI Content Coming Soon')),
    );
  }
}

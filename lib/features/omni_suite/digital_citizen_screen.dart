import 'package:flutter/material.dart';

class DigitalCitizenScreen extends StatelessWidget {
  const DigitalCitizenScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Digital Citizen')),
      body: const Center(child: Text('Digital Citizen Content Coming Soon')),
    );
  }
}
